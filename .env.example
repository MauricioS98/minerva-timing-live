# PostgreSQL — Minerva Timing Live
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=minerva_timing_live

# Alternativa:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/minerva_timing_live

PORT=4000
